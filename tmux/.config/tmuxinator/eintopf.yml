# /home/paul/.config/tmuxinator/eintopf.yml

name: eintopf
root: ~/Dev/github.com/relnod/eintopf

windows:
  - EINTOPF:
      layout: main-vertical
      panes:
        - nvim
        - make watch-api
        - make watch-web
        - 
